/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode:true,
    compiler:{
        // styledComponents有効化
        styledComponents:true
    }
}

module.exports = nextConfig
